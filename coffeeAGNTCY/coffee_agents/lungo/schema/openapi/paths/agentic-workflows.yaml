# Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

# All path items for the Agentic Workflows API - single surface.

paths:
  /:
    get:
      tags: [agentic-workflows]
      operationId: redirectRootToPatterns
      summary: Redirect API root to patterns catalog
      description: >
        Default entry point; redirects to GET /patterns/ (temporary redirect).
      responses:
        "307":
          description: Temporary redirect to the patterns list
          headers:
            Location:
              required: true
              schema:
                type: string
                example: /patterns/

  /patterns/:
    get:
      tags: [agentic-workflows]
      operationId: listPatterns
      summary: List patterns
      responses:
        "200":
          description: List of patterns
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/PatternListResponse

  /patterns/{name}/chat:
    post:
      tags: [agentic-workflows]
      operationId: patternChat
      summary: Chat with a pattern's reference docs
      description: >
        NDJSON stream grounded in the named pattern's markdown. One
        {"response": "..."} per chunk; terminated by {"done": true} or
        {"error": "..."}. Session state held in memory keyed by
        (pattern_name, session_id).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ../components/schemas.yaml#/components/schemas/PatternChatRequest
      responses:
        "200":
          description: NDJSON stream of response chunks.
          content:
            application/x-ndjson:
              schema:
                type: string
        "404":
          description: No reference material for the given pattern name.
        "422":
          description: Request body failed validation.

  /use-cases/:
    get:
      tags: [agentic-workflows]
      operationId: listUseCases
      summary: List use-cases
      responses:
        "200":
          description: List of use-cases
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/UseCaseListResponse

  /pattern-categories/:
    get:
      tags: [agentic-workflows]
      operationId: listPatternCategories
      summary: List pattern categories
      responses:
        "200":
          description: List of agentic design pattern categories
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/PatternCategoryListResponse

  /pattern-categories/{category_name}/documentation/:
    get:
      tags: [agentic-workflows]
      operationId: getPatternCategoryDocumentation
      summary: Get pattern category documentation (markdown)
      parameters:
        - name: category_name
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Category reference markdown
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/PatternCategoryDocumentationResponse
        "404":
          description: Category documentation not found

  /agentic-workflows/:
    get:
      tags: [agentic-workflows]
      operationId: listAgenticWorkflows
      summary: List workflows (optional filters)
      parameters:
        - name: patterns
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
          description: Filter by pattern names
        - name: use_cases
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
          description: Filter by use-case names
        - name: pattern_categories
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
          description: Filter by pattern category display names
      responses:
        "200":
          description: Available workflows for the optional filters, keyed by workflow name
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/WorkflowSummaryMapResponse

  /agentic-workflows/{workflow_name}/:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
        description: Workflow name (matches Workflow.name in the event contract)
    get:
      tags: [agentic-workflows]
      operationId: getAgenticWorkflow
      summary: Workflow details (optionally topology only)
      parameters:
        - name: topology_only
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: >
            Workflow definition including starting topology and instances map.
            When topology_only is true, servers may return a topology-focused projection.
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/Workflow
    post:
      tags: [agentic-workflows]
      operationId: instantiateAgenticWorkflow
      summary: Instantiate a workflow
      responses:
        "200":
          description: New workflow instance id (InstanceId URI)
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/InstantiateWorkflowResponse

  /agentic-workflows/{workflow_name}/documentation/:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
        description: Workflow name (matches Workflow.name in the event contract)
    get:
      tags: [agentic-workflows]
      operationId: getWorkflowDocumentation
      summary: Workflow documentation (markdown, structured sections)
      description: >
        Returns markdown from docs/workflows for this catalog workflow name.
        The server maps the name to a basename slug to find the .md file, then splits
        at ## headings for structured rendering.
      responses:
        "200":
          description: Parsed workflow documentation
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/WorkflowDocumentationResponse
        "404":
          description: Unknown workflow name or no markdown file for that name

  /agentic-workflows/{workflow_name}/instances/:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
    get:
      tags: [agentic-workflows]
      operationId: listWorkflowInstances
      summary: List instances for a workflow
      responses:
        "200":
          description: Workflow instances for this workflow, keyed by instance id (InstanceId URI)
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/WorkflowInstanceMapResponse

  /agentic-workflows/{workflow_name}/instances/{workflow_instance_id}/:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
      - name: workflow_instance_id
        in: path
        required: true
        schema:
          $ref: ../components/schemas.yaml#/components/schemas/WorkflowInstancePathId
    get:
      tags: [agentic-workflows]
      operationId: getWorkflowInstanceState
      summary: Workflow instance state (full or topology only)
      parameters:
        - name: topology_only
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Instance state or topology projection
          content:
            application/json:
              schema:
                $ref: ../components/schemas.yaml#/components/schemas/WorkflowInstance
    delete:
      tags: [agentic-workflows]
      operationId: deleteWorkflowInstance
      summary: Delete workflow instance
      description: >
        Schedules removal from the in-memory store. Existing instance returns 202;
        already absent instance returns 204 (idempotent). Unknown workflow returns 404.
      responses:
        "202":
          description: Delete accepted
        "204":
          description: No instance to delete (already removed or never existed)
        "404":
          description: Workflow not found

  /agentic-workflows/{workflow_name}/instances/{workflow_instance_id}/events/:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
      - name: workflow_instance_id
        in: path
        required: true
        schema:
          $ref: ../components/schemas.yaml#/components/schemas/WorkflowInstancePathId
    post:
      tags: [agentic-workflows]
      operationId: postWorkflowInstanceEvent
      summary: Apply a workflow instance state update event (internal)
      description: >
        Internal middleware / A2A path: updates workflow instance state from an Event message.
        Not intended for untrusted public clients.
      x-internal: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ../components/schemas.yaml#/components/schemas/Event
      responses:
        "204":
          description: Event accepted
        "400":
          description: Invalid event payload

  /agentic-workflows/{workflow_name}/instances/{workflow_instance_id}/events/stream:
    parameters:
      - name: workflow_name
        in: path
        required: true
        schema:
          type: string
          minLength: 1
      - name: workflow_instance_id
        in: path
        required: true
        schema:
          $ref: ../components/schemas.yaml#/components/schemas/WorkflowInstancePathId
    get:
      tags: [agentic-workflows]
      operationId: streamWorkflowInstanceEvents
      summary: SSE stream of workflow instance events
      responses:
        "200":
          description: Server-Sent Events stream
          content:
            text/event-stream:
              schema:
                type: string
                description: Opaque SSE payload (event frames)
